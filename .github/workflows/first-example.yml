name: My Workflow

on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "My Github Actions"

        - name: List Files
          run: ls

        - name: read Files
          run: cat README.md